<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#EBE8EE">

    <!-- Set a basic background color -->
    <View
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:background="#3F51B5" />

    <!-- Set a basic header color -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#3F51B5"
        android:padding="16dp"
        android:text="Navigation Menu"
        android:textColor="#82B1FF"
        android:textSize="24sp" />

    <!-- Appointment Button -->
    <Button
        android:id="@+id/appointmentButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:background="#4CAF50"
        android:onClick="navigateToAppointment"
        android:padding="16dp"
        android:text="Appointment"
        android:textColor="#263238" />

    <!-- Services List Button -->
    <Button
        android:id="@+id/servicesButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:background="#4CAF50"
        android:onClick="navigateToServices"
        android:padding="16dp"
        android:text="Services List"
        android:textColor="#263238" />

    <!-- Navigation List -->

</LinearLayout>